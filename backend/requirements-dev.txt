# Test-only dependencies. Not installed in production.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
# httpx is a runtime dependency now (Ollama); requirements.txt pins it at
# 0.27.2, which is also what starlette's TestClient needs -- fastapi 0.104
# passes app= to httpx.Client, removed in httpx 0.28.
